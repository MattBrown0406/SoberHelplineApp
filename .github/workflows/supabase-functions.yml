name: Supabase Functions

on:
  push:
    branches: [main]
    paths:
      - 'supabase/functions/livekit-token/**'
      - 'supabase/functions/livekit-remove/**'
      - 'supabase/functions/send-engagement-push/**'
      - 'supabase/functions/zoom-sync/**'
      - 'supabase/functions/sync-web-membership/**'
      - '.github/workflows/supabase-functions.yml'

jobs:
  deploy-functions:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Deploy functions
        run: |
          supabase link --project-ref ${{ vars.SUPABASE_PROJECT_REF }}
          supabase functions deploy livekit-token
          supabase functions deploy livekit-remove
          supabase functions deploy send-engagement-push
          supabase functions deploy zoom-sync
          supabase functions deploy sync-web-membership
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
